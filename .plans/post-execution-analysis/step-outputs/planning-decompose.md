All checks passed and committed. The `single-step` workflow dispatches any named template as a standalone step, registered as `step` in the workflow registry.

DONE VNM-34.101 Add single-step workflow flow that dispatches a named template parameter as a standalone step