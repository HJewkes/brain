// Re-exports from the shared design token layer.
// All existing `C` and `TOOL_COLORS` imports continue to work unchanged.
export { colors as C, TOOL_COLORS, palette, semantic, component } from '../../tokens.js';
